import { Box, Typography } from '@mui/material'

const MusicRoomsContainer = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="body2" color="text.secondary">
      music rooms container — placeholder, wire up RTK Query hooks +
      components/music-rooms/* here once the feature is built
    </Typography>
  </Box>
)

export default MusicRoomsContainer

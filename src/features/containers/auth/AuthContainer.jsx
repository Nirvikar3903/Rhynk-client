import { Box, Typography } from '@mui/material'

const AuthContainer = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="body2" color="text.secondary">
      auth container — placeholder, wire up RTK Query hooks + components/auth/*
      here once the feature is built
    </Typography>
  </Box>
)

export default AuthContainer

import { Box, Typography } from '@mui/material'

const ConversationsPlaceholder = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="body2" color="text.secondary">
      conversations — placeholder, replace with real UI once this feature is built
    </Typography>
  </Box>
)

export default ConversationsPlaceholder

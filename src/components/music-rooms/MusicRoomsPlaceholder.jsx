import { Box, Typography } from '@mui/material'

const MusicRoomsPlaceholder = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="body2" color="text.secondary">
      music rooms — placeholder, replace with real UI once this feature is built
    </Typography>
  </Box>
)

export default MusicRoomsPlaceholder
